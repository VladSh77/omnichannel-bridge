name: CI

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  bootstrap-check:
    name: Bootstrap Check
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Validate workflow startup
        run: |
          echo "workflow started"
