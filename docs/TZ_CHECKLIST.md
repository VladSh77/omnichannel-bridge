# Технічне завдання: омніканальний відділ продажів у Odoo (чекліст)

**Продукт:** Fajna — омніканальна система комунікацій і продажів на базі Odoo.  
**Мета:** не «черговий костиль», а **повноцінна платформа рівня сильного відділу продажів**: дані з Odoo, гнучкий бот з ШІ, контроль якості діалогу, передача гарячого клієнта менеджеру, масштабування каналів.

**Легенда статусів**

| Символ | Значення |
|--------|----------|
| `[x]` | Реалізовано в репозиторії (поточний стан `omnichannel_bridge`) |
| `[~]` | Частково / заглушка / потребує доробки під ваш бізнес |
| `[ ]` | Не реалізовано / заплановано |

**Статус-зріз (2026-04-09):**

- `[x]` — 160
- `[~]` — 0
- `[ ]` — 0

**Контекст:** паралельно може існувати **SendPulse + Odoo**; цей проєкт — **нове підключення** (окремі вебхуки в Odoo), **міграція не обов’язкова**. Мета — **прибрати обмеження** сценарних ботів без ШІ (повтори питань, глюки, дратування клієнтів, довге очікування) і дати **гнучкість і якість** під продажі **на відкритому стеку** (без купівлі модулів на маркетплейсі; LLM — локальний Ollama або опційно хмара).

## Операційний інцидент доступу (2026-04-07)

- Під час hardening SSH виник lockout для поточного automation-середовища через mismatch ключа на сервері та ключа агента.
- Коригувальні дії: відновлено доступ по актуальному pubkey агента, зафіксовано правило порядку змін доступу.
- Обов’язковий порядок на майбутнє:
  1. підтвердити вхід ключем з активного automation-середовища;
  2. лише після цього вимикати password auth;
  3. повторно перевіряти key-login після рестарту sshd.

---

## Домен бізнесу: путівки в дитячі табори (преміум)

**Позиціонування:** преміум — тон спілкування **ввічливий, спокійний, без тиску**; бот і менеджер не «продають в лоб», а супроводжують рішення батьків щодо **дітей**.

**Продукт у Odoo (узгоджена модель):**

- Табір — **послуга / товар**, прив’язаний через **квиток** до **події** (`event` / `website_event` або ваша кастомна схема — зафіксувати в техдокі).
- **Ціни** та умови відображені в **договорі публічної оферти** на сайті; бот **не вигадує** ціни — лише посилається на факти з Odoo + URL документів.
- **Публічний лендінг кампанії** [lato2026.campscout.eu](https://lato2026.campscout.eu/) містить багато базової інформації (програми, тон, контакти, дедлайни на сторінці); використовувати в ТЗ і в промптах як **маркетинговий і навігаційний** шар. **Числа в відповіді клієнту в чаті** (ціни, місця, оплата) — тільки з Odoo, деталі в `docs/LEGAL_FACTS_PACK.md`.

### Інтеграція з існуючим Odoo: читати код на сервері, не «вгадувати» схему

**Чому так:** у продакшені вже є **власні кастомні модулі**, яких **немає в базовому (стандартному) Odoo**. Саме вони часто визначають, як рахуються **місця, квитки, реєстрації, знижки** тощо. Модуль `omnichannel_bridge` **має співпрацювати з цим кодом**, а не будуватися лише під «типові» моделі Community/Enterprise — тому схему даних і бізнес-правила знімають **безпосередньо з вашого репозиторію аддонів і БД на сервері**, а не з загальних підручників.

Зв’язок **квиток → подія → зайняті / вільні місця** і будь-які обчислення квот **не визначаються з одного лише ТЗ** чи документації «чистого» Odoo. Реальність інстансу формують:

- **Ваші кастомні модулі** (пріоритет при аналізі) + **сторонні** (OCA, інтеграції) — нові/змінені моделі, `inherit`, `related`, `compute`, обмеження SQL.
- **Переклади в інтерфейсі** (українська/польська) можуть бути неконсистентними або вводити в оману — **надійні технічні імена** лише в **Python/XML** і в ORM.
- «Поганий переклад» не скасовує фактичну логіку в коді; інтеграція мусить орієнтуватися на **реальні поля та методи**, а не на підписи в формі.

**Обов’язкова практика розробки (на staging або production-клоні з тим самим кодом):**

1. Переглянути **встановлені додатки** (`Apps`) і `addons_path` — окремо виділити **ваші кастомні** пакети (не з upstream Odoo).
2. Прочитати **вихідний код** цих модулів і залежностей (`models/*.py`): `_name`, `inherit`, поля, `compute`, домени пошуку, стани (`state`, `active`), скасування броні.
3. За потреби — **Odoo shell** / дебаг: `env['model']._fields`, тестовий `search_count` для однієї події, звірка з UI.
4. Результат зафіксувати в **внутрішній техдок** репозиторію або wiki: таблиця «модель / поле / що означає / як рахується вільне місце / коли рядок блокує місце».
5. Після **оновлення Odoo, оновлення/додавання модуля** — повторити перевірку (регресія інтеграції).

**Чекліст:**

- [x] Є документ **«Мапінг табори / місця»** з посиланнями на **конкретні кастомні модулі** та файли на сервері: `docs/PROD_CAMP_MAPPING_AUDIT_2026-04-08.md`.
- [x] Перевірено **скасовані / чернетки / очікуючі** реєстрації на прод-клоні (`event.registration.state` + spot-check по подіях).
- [x] Резервний список реалізовано як окрема модель `omni.reserve.entry` (зв'язок з partner/channel/lead), інтегровано в sold-out runtime flow.
- [x] Інтеграційні тести або ручний сценарій на **копії прод-БД** перед увімкненням у проді (додано `docs/PROD_COPY_MANUAL_SCENARIOS.md`).

**Місця та резерв:**

- [x] Python/ORM перевірка вільних місць: runtime resolver у `omni_knowledge` підтверджено прод-аудитом (`get_camp_availability` / `bs_event_id.seats_available` / `event.ticket -> event.seats_available`).
- [x] Якщо місць **немає**: runtime reserve flow через менеджера реалізовано (CTA клієнту + CRM lead + escalation notify + stage=handoff).
- [x] Анти-галюцинація по місцях: facts bundle містить тільки ORM-derived `places`; при `places<=0`/`unknown` додається `reserve: manager_waitlist_required` і strict reserve policy у prompt.

**Знижка −5% (лише на табір):**

- [x] Клієнт отримує **номер купона**; вводить його при **реєстрації / оформленні**: додано `sale.order.omni_coupon_code` + валідацію коду.
- [x] У Odoo: реалізовано public-coupon flow (код + % в Settings), автоматичне застосування знижки лише до camp lines (`bs_event_id` / event-type).
- [x] Облік використання купона: `omni.coupon.redemption` (one-use-per-partner-per-code) + campaign linkage (`promo_id`, `campaign_code`) + `valid_until` з promo-window.

**Організація продажів:**

- [x] **Один менеджер** — графік **негнучкий** («як виходить»); автоматика компенсує **ніч/ранок/вихідні/затримки** через schedule+quiet+session lock+fallback.

**Мови:**

- [x] Відповідь **українською** або **польською** — залежно від мови звернення клієнта (runtime-детекція + фіксація `omni_detected_lang` на треді).
- [x] **Російська мова для відповідей не використовується** (runtime-policy: при RU/BE зверненні надсилається нейтральна відповідь UA/PL про мову спілкування).

**Юридичне та RODO (ЄС, робота з дітьми):**

- [x] У чаті — **коротке пояснення згоди** на обробку даних (контролер/мета/посилання) — авто-блок у першому bot message на канал.
- [x] Бот має в контексті legal-блок (затверджені URL terms/privacy/cookie/child-protection + правило без вигаданих юридичних текстів).
- [x] Відповідає **фірма** (юридична особа) — у consent-блоці вказується назва юрособи з `company.name`.
- [x] Підвищений рівень обов’язків через **дітей** — базова автоескалація до людини при чутливих питаннях (ключові слова UA/PL/EN).

**FOMO / термінові офери:** долучаються **окремим** підпроєктом (див. § 12), щоб не суперечити преміум-тону.

---

## 1. Стратегія та відмова від «костиля»

- [x] Один технічний контур: webhook → Odoo → Discuss → (опційно) відповідь у месенджер
- [x] Логіка на Python у кастомному модулі Odoo (не лише зовнішній SaaS як єдиний мозок)
- [x] **Нове підключення** поруч з існуючими інтеграціями (без вимоги міграції SendPulse)
- [x] **Відкритий стек:** Odoo LGPL + власний модуль; рекомендований LLM — **Ollama** + модель з відкритими вагами; без залежності від платних Odoo Apps для ядра
- [x] **SLA (узгоджено з доменом):** якщо менеджер **не відповів протягом ~3 хвилин** після останнього повідомлення клієнта — бот може підхопити діалог (додано `sla_scope`: робочий час або 24x7).
- [x] **Навантажувальні** критерії (одночасні діалоги, пік реклами) — baseline зафіксовано в `docs/LOAD_CRITERIA.md`.

---

## 2. Канали та пріоритети

- [x] **Meta (Facebook + Instagram)** — прийом повідомлень (webhook POST), верифікація GET, підпис App Secret, відправка відповіді (Graph API)
- [x] **Telegram** — прийом і відправка (Bot API)
- [x] **WhatsApp** (WABA / Twilio тощо) — додано runtime parser/outbound для WhatsApp Cloud API і Twilio-специфічний inbound parser (`_omni_process_twilio_whatsapp`).
- [x] **Viber** — runtime inbound/outbound реалізовано (message event parser + optional signature verify + send_message outbound)
- [x] **Чат на сайті** — нативна інтеграція з Odoo Live Chat у спільний контур + зафіксований prod smoke (`docs/PROD_LIVECHAT_SMOKE_2026-04-08.md`).
- [x] Пріоритет обробки **Meta перед** іншими у маршрутизаторі webhook

### 2.1 Вікна повідомлень (політика платформ)

- [x] Зафіксовано в експлуатаційній документації: **Meta (Messenger / Instagram Direct), WhatsApp Business** — типово **вікно ~24 години** для проактивних повідомлень після останнього **вхідного** повідомлення клієнта (умови залежать від продукту Meta / WABA; перевіряти актуальні правила Business / Developer docs).
- [x] **Telegram** — **немає** такого ж обмеження для бота: можна писати поза 24-годинним циклом (з урахуванням антиспаму та згоди клієнта).
- [x] Юридично/продуктово: текст **згоди** на повідомлення в кожному каналі (особливо маркетинг у Telegram) — керовані consent-поля в Settings + `CHANNEL_CONSENT_POLICY` у grounding.

### 2.2 Вхід у діалог на сайті: pre-chat або «розумні» кнопки (узгоджено з продуктом)

**Проблема, яку закриваємо:** у неробочий час (і часто на першому кроці) користувачі бачать лише кнопки без вільного вводу або навпаки — одразу відкритий чат без контексту. Потрібен передбачуваний UX: **або збір контакту + згода, або кнопки, що дають реальну цінність**, і завжди можливість **залишити контакт**.

**Еталон UX (нативний Odoo Website Live Chat / чатбот):** одне повідомлення бота з питанням, під ним **вертикальний список кнопок-відповідей** (quiz), а внизу віджета **завжди видимий рядок вводу** («Скажіть щось…») — користувач може або натиснути варіант, або одразу написати власний текст. Це **цільовий вигляд** для CampScout: не допускати стану, коли замість поля вводу показується лише підказка на кшталт «Виберіть опцію вище» без композера.

**Два допустимі варіанти (обрати один як основний або комбінувати за правилами каналу):**

1. **Pre-chat / стартова форма (аналог SaaS «pre-chat form»)**  
   - Перед повноцінним полем вводу показується короткий блок: ім’я + **хоча б один** з каналів зв’язку (email **або** телефон), опційно — причина звернення.  
   - Поруч — **коротке RODO-пояснення** (контролер, мета, посилання на політику; без вигаданого юридичного тексту в боті).  
   - Після відправки форми відкривається **повний чат** (вільний текст + AI/оператор за правилами каналу).  
   - У Odoo: реалізація через **кроки чатбота Live Chat**, окремий **website snippet** або кастомний крок у `omnichannel_bridge` — технічний вибір фіксується в техдокі після оцінки.

2. **Кнопки як навігація по цінності, не як «глухий коридор»**  
   Якщо на першому екрані використовуються кнопки, кожна гілка має вести до **конкретної користі**, а не лише до загального «привіт»:  
   - **Про компанію CampScout** — 2–4 речення + посилання на затверджені сторінки (про нас, ліцензія/довіра — лише з фактів сайту/Odoo).  
   - **Табори / зміни** — короткий огляд + посилання на каталог/події на сайті; у чаті далі — уточнення віку, дат, бюджету (узгоджено з sales discovery).  
   - **Інші послуги** (якщо є в продуктовій лінійці) — окремі кнопки або підменю, щоб не змішувати з табором без явного вибору.  
   - **Ціни / умови** — відповідь лише з **FACTS з Odoo** + посилання на оферту; без вигаданих цифр.  
   - **Залишити контакт / передзвін / email** — **обов’язкова** гілка або фінальний крок після будь-якої з гілок: ім’я + email або телефон, підтвердження згоди.  
   - **Одночасно з кнопками** має лишатися доступним **повний композер** (як у еталоні Odoo вище); додаткова кнопка «Написати своє питання» — опційна, якщо з коробки вже є вільний ввід.

**Продуктові правила (обов’язково):**

- [x] Зафіксовано в макеті чату **дерево кнопок** і тексти (UA + PL), погоджено з маркетингом/юристом (`docs/LIVECHAT_ENTRY_UX_TREE.md`).  
- [x] Віджет і кроки чатбота налаштовані так, щоб **рядок вводу не ховався** під час показу кнопок (відповідає еталону Odoo: питання + кнопки + «Скажіть щось…»).  
- [x] У неробочий час: або **pre-chat**, або перший крок бота збирає контакт **до** довгого діалогу (узгодити з § 6.1 «бот vs менеджер»).  
- [x] Жоден сценарій не залишає відвідувача **без** можливості залишити контакт і **без** вільного тексту там, де очікується діалог (кнопки не замінюють композер, якщо не ухвалено окремий лише-form сценарій).  
- [x] Дані з форми/кнопок потрапляють у **res.partner** / **CRM lead**: додано pre-chat entry sequence (name + contact) у livechat runtime + lead creation при контакті.  
- [x] Логіка **ШІ** після збору контакту: не дублювати вже відомі поля з картки (анти-повтор з § 3).  

**Референси (відкриті аналоги на GitHub / індустрія):** pre-chat форми у [Chatwoot](https://github.com/chatwoot/chatwoot), offline/start form у [Live Helper Chat](https://github.com/LiveHelperChat/livehelperchat) — як орієнтир UX, не як обов’язковий стек.

---

## 3. Облік клієнта в Odoo (не питати зайве)

- [x] Пошук / створення **res.partner** за зовнішнім id каналу (`omni.partner.identity`)
- [x] Пошук існуючого клієнта за **телефоном** і **e-mail** при першому контакті
- [x] Витяг **e-mail і телефону з тексту** повідомлення; переприв’язка ідентичності до знайденого партнера
- [x] Оновлення треду Discuss при зміні «кращого» партнера
- [x] **Анти-повтор:** не питати поле, яке вже заповнене в партнері / у стані звернення (runtime prefill з inbound + next-question тільки по missing slots).
- [x] Явна **модель стану діалогу** (FSM / етап кваліфікації): guard-метод `res.partner.omni_set_sales_stage` + журнал переходів `omni.stage.event` + editor/UI `omni.stage.transition`.
- [x] Злиття дублікатів партнерів за правилами (не лише переприв’язка identity) — `omni_merge_duplicates_by_rules`.

### 3.1 Точні факти, звернення, «навчання» без галюцинацій

- [x] Блок **FACTS_FROM_DATABASE** збирається **лише в Python** (ORM): картка клієнта, каталог, оплати, звернення
- [x] Режим **суворого підґрунтя** (інструкція моделі: не вигадувати ціни, місця, договори)
- [x] Поля партнера: **зворот** (`omni_addressing_vocative`), **стиль** (пані/пане/на ти), **пам’ять** (`omni_chat_memory`)
- [x] Шаблони привітання українською з підстановкою звороту (за стилем)
- [x] Витяг з повідомлень (regex, без ML): «називайте мене …», «на ти» → оновлення полів і рядок у пам’яті
- [x] Невеликий словник типових імен → зворот (розширено + керований override `vocative_map_extra` у Settings); повна морфологія UA — за потреби окремий OSS-модуль
- [x] Останні повідомлення треду в контексті як **RECENT_THREAD** (не фінансова правда)
- [x] **Анти-повтор** питань по полях CRM (адреса тощо) — посилено: prefill полів з inbound і перевірка поточного тексту перед постановкою next-question.
- [x] **RAG / база знань** з документів (страхування тощо) — додано `RAG_CONTEXT` із релевантних legal/insurance записів у grounding.

---

## 4. Дані для бота: товари, ціни, місця, умови

- [x] Поля на **product.template**: квота місць / місць для оферу (`omni_places_remaining`), текст умов для бота (`omni_chat_terms`)
- [x] Формування **контексту каталогу** для LLM (назва, ціна з прайс-листа партнера, місця/залишок, умови)
- [x] Фільтр «як на сайті»: якщо встановлено `website_sale`, використовується **`is_published`**
- [x] **Табори як події + квитки:** синхронізація «вільних місць» з **event / реєстраціями** (runtime truth-sync через `event.registration` + fallback на `seats_available`/custom helpers).
- [x] **Резервний список** при sold-out: окрема модель `omni.reserve.entry` + повідомлення клієнту та менеджеру + handoff.
- [x] **Купон −5%:** додано обмеження на категорії товарів (`coupon_allowed_categ_ids` у Settings, включно з підкатегоріями) + валідація при замовленні.
- [x] Окремі сутності **акцій / промо**: модель `omni.promo` + UI (list/form) + факт-блок `PROMOTIONS` у grounding bundle.
- [x] **Страхові договори / пакети** як товари або окремі записи з політиками показу боту (додано модель `omni.insurance.package` + UI + контекст `INSURANCE_PACKAGES`).
- [x] Вкладення / PDF **за політикою** (реєстр `omni.legal.document` з `is_pdf` + `allow_in_bot`, контекст `LEGAL_DOCUMENTS`).
- [x] **Юридичний пакет у контексті бота:** посилання + короткі затверджені формулювання (оферта, RODO, cookies, захист дітей) — без вигадування юридичного тексту ШІ (керовані поля в Settings + `LEGAL_CONTEXT`).

---

## 5. Оплати та «клієнт уже платив»

- [x] Зведення по **рахунках** (`account.move`): оплачено / в оплаті / без оплат
- [x] Платежі **онлайн** (`payment.transaction`) і зв’язок із замовленням: notify-тригери + payment/reconciliation журнал `omni.payment.event` (sale/payment/invoice джерела).
- [x] Правила формулювань для бота (що саме можна казати про оплату з юридичної точки зору): додано `PAYMENT_POLICY` у strict grounding bundle.

---

## 6. ШІ та «навчання» бота (не один разовий промпт)

- [x] Бекенд **Ollama** (локально, `/v1/chat/completions` або `/api/chat`) — **рекомендований OSS-шлях**
- [x] Опційно **OpenAI Chat Completions** (пропрієтарний API)
- [x] **System prompt** + єдиний блок **omni_strict_grounding_bundle** (каталог, оплати, картка, звернення, пам’ять, тред)
- [x] Режими відповіді: **завжди / лише поза годинами менеджера / вимкнено**
- [x] Години менеджера (інтервал + timezone компанії)
- [x] «Навчання» без fine-tuning: **пам’ять на картці** + правила з тексту + policy `docs/LEARNING_POLICY_NO_FINETUNE.md` (без auto retrain ваг).
- [x] **База знань** (статті FAQ, політики, страхування) з окремого UI + RAG/сегментована подача: додано керований UI `omni.knowledge.article` + включення в `RAG_CONTEXT`.
- [x] Версіонування промптів і A/B або черги експериментів (baseline: `llm_prompt_version` + `llm_experiment_tag` у Settings і `PROMPT_VERSIONING` в grounding).
- [x] Fine-tuning / окремий ассистент — за потреби, не як заміна даним Odoo (baseline assistant profiles: `default/sales_closer/support_safe`).
- [x] Модерація / заборонені теми / ескалація при ризику (додано policy-engine `omni.moderation.rule` + action routing `escalate/escalate_pause/note_only`).

### 6.1 Гібрид «бот ↔ один менеджер» (ніч, 3 хв, пауза)

**Контекст:** один менеджер, графік нерегламентований — бот закриває **ніч**, **дуже ранок**, паузи та **відсутність відповіді ~3 хв**; коли менеджер активний — бот **не конкурує** за тон спілкування.

- [x] **Ніч / ранок:** опційне вікно `night_bot_*` + затримка черги під час робочих годин менеджера на SLA (`bot_inside_hours_if_manager_quiet`, `omni_autoreply_delay_seconds_for_inbound`) — Meta/Telegram; live chat без зміни.
- [x] **Таймер ~3 хв:** після повідомлення клієнта, якщо за N секунд **немає** відповіді від людини (не OdooBot) у треді — дозволити відповідь бота (черга/cron + перевірка `mail.message`).
- [x] **Пауза бота при менеджері:** якщо менеджер **написав** у треді, запускається manager-session lock (`manager_session_active`) з timeout; у цей період inbound не enqueue-иться в LLM.
- [x] **Миттєве підхоплення:** опція «бот відповідає одразу, менеджер підключається пізніше» — сумісна з паузою після першої репліки менеджера.
- [x] Логування: хто відповів (бот / менеджер), щоб аналізувати якість (додано CRM Analytics метрики `bot_reply_threads` / `human_reply_threads` / `mixed_reply_threads`).

---

## 7. Кращі практики продажів і якість комунікації

- [x] Внутрішні ноти з **підказками дефіциту** (FOMO за залишком / квотою) + внутрішній notify для гарячих кейсів з cooldown.
- [x] Скрипти **виявлення болю**, заперечень, upsell — як керовані блоки (`pain_script`, `upsell_script`), не лише текст у промпті.
- [x] **Качество менеджера:** підказка/перефраз перед відправкою (граматика, тон) — wizard `omni.manager.reply.assist`.
- [x] Шаблони відповідей менеджера з підстановкою з Odoo (модель `omni.manager.reply.template`).
- [x] Оцінка **«гарячості»** ліда (скоринг) і тригери CRM (`res.partner.omni_lead_score` + recompute triggers).

### 7.0 Тон і мови (преміум, табори)

- [x] Стиль відповідей: **теплий, повагою до батьків**, без агресивних тригерів (`STYLE_POLICY` + керований override у Settings).
- [x] **Українська / польська** за мовою клієнта; **російська для відповідей не використовується**.
- [x] Детекція мови (простий класифікатор / ключові слова / перше повідомлення) + fallback UA.
- [x] Окремі системні інструкції (prompt) для PL поруч з UA в налаштуваннях або в коді за `lang`.
- [x] UI локалізація модуля: робочі мови **українська + польська**, англійська лише fallback для технічних/неперекладених рядків.
- [x] Після кожного релізу: `-u omnichannel_bridge` + update translations (UA/PL) як обов'язкова post-deploy процедура.

### 7.1 Робота з запереченнями (методологія для бота та менеджера)

*Матеріал для навчання команди; реалізація в модулі — чекліст нижче.*

**Навіщо окремий блок:** заперечення — головна причина втрати угоди; сценарний бот часто «ламається» або повторює офер. Тут потрібні **правила + дані з Odoo**, а не вільна фантазія ШІ.

**Типові заперечення (класифікація для діалогу):**

| Тип | Приклади | Логіка відповіді |
|-----|-----------|------------------|
| **Ціна / бюджет** | «Дорого», «не готовий платити стільки» | Підтвердити емпатію → **факти** з Odoo (прайс-лист, складники цінності, розстрочка/етапи оплати — лише якщо є в системі) → запитання на уточнення бюджету / порівняння з альтернативою |
| **Час** | «Пізніше», «після відпустки» | Зафіксувати дату в CRM → легкий наступний крок (нагадування в **дозволеному вікні** каналу або перехід у Telegram) |
| **Довіра** | «Не знаю вашу компанію», «перший раз чую» | Соціальний доказ **лише правдивий** (реальні відгуки/цифри з дозволених джерел); без вигаданих нагород |
| **Потреба в «подумати»** | «Я подумаю» | Уточнити, над чим саме сумнів → одне чітке питання; запропонувати менеджера або коротке резюме переваг **з фактів** |
| **Конкурент** | «У конкурента дешевше» | Не обговорювати конкурента на емоціях; перенос на **вашу пропозицію** (що входить, гарантії, умови з Odoo) |
| **Не я ЛПР** | «Треба порадитись з …» | Запитати роль ЛПР, зручний час дзвінка/чату, **handoff** менеджеру |

**Принципи (кращі практики):**

1. **Спочатку прийняти** («розумію, це важливе рішення»), не сперечатися з першої репліки.
2. **Ізолювати** заперечення: «Якщо вирішимо питання ціни, зручно буде продовжити?»
3. **Питання краще тверджень** — щоб не звучати тиском.
4. У відповідях бота — **лише підтверджені дані** (блок FACTS); якщо аргументу немає в Odoo — чесно сказати і запропонувати людину.
5. **Ескалація:** при юридичних/медичних/страхових питаннях або агресії — одразу **менеджер**, бот не імпровізує.

**Чекліст реалізації в продукті:**

- [x] Детектор **типу заперечення** (ключові фрази + легкий класифікатор): додано rule+score classifier (phrase/token scoring) у `omni.sales.intel`.
- [x] Прив’язка до **готових блоків відповіді**: `OBJECTION_PLAYBOOK` у system prompt + Settings + окремий CRM/UI policy-editor (`omni.objection.policy`).
- [x] Після відпрацювання заперечення — **наступний крок** у воронці (наприклад, мікро-зобов’язання клієнта)
- [x] Лог у партнера: **які заперечення** вже були — рядки `objection:*` в `omni_chat_memory` + внутрішня нота в Discuss.
- [x] Метрики: частота типів заперечень, конверсія після відпрацювання (додано `objection_to_intent_percent` у CRM Analytics).

---

## 8. Handoff: гарячий клієнт → менеджер

**Обов’язкова ескалація до людини (узгоджено для таборів):**

- [x] **Конфлікт**, **непорозуміння**, явний **запит людини** (runtime detection + problematic notify).
- [x] **Технічні проблеми** (оплата, сайт, купон, реєстрація) — runtime detection + problematic notify.
- [x] Клієнт **оформлює покупку** / готовність до оплати — purchase-intent + PRIORITY notify + stage→handoff + payment-confirmed фіксація (`omni_purchase_confirmed_at`).
- [x] **Поза темою** або **повторні помилки / плутанина** в повідомленнях — щоб не «ломати» довіру преміум-бренду.
- [x] Будь-які **чутливі** питання щодо **дітей**, здоров’я, безпеки — пріоритет менеджеру (детектор у `omni.ai`).

**Реалізація:**

- [x] Внутрішня нота при ключових словах («менеджер», «людина»)
- [x] Відповіді в Discuss від оператора **йдуть назад у Meta / Telegram**
- [x] Для **одного менеджера** — пряме призначення / нотифікація: `default_manager_user_id` + auto owner assignment + `mail.activity`.
- [x] **@згадка** / push / e-mail на менеджера при тригерах вище: внутрішній TG + direct activity + optional email notify.
- [x] SLA **~3 хв** — узгоджено з паузою бота через `sla_scope` (manager_hours / always) і delay-чергу.

### 8.1 Внутрішній Telegram-канал для менеджера та керівника (операційний)

**Окремо від** клієнтського бота та розсилок (див. § 13): це **закритий канал/чат у Telegram**, де перебувають **менеджер** і **керівник** (та за потреби обмежене коло осіб). Сюди **стікається інформація по всіх клієнтах** з омніканалу — щоб команда бачила потік звернень без постійного сидіння в Discuss.

**Продуктові правила:**

- [x] У канал потрапляє **підсумок події**: unified summary для new/escalation/problem/stage-change/purchase-intent + purchase-confirmed (ORM `sale.order` state sale/done).
- [x] **Окремий акцент / окремий тип повідомлень**: `PRIORITY` формат для problematic/escalation + optional `internal_tg_priority_chat_id` (другий канал).
- [x] Керівник одразу бачить **пріоритетні** кейси: реалізовано префікс `PRIORITY` і optional окремий чат + keyword override (`internal_notify_priority_keywords`).

**Технічна реалізація (чернетка):**

- [x] Існуючий внутрішній бот + `chat_id` у `ir.config_parameter`; додано optional `internal_tg_priority_chat_id` для пріоритетних кейсів.
- [x] Виклик з Odoo після відповідних подій: `requests.post` до Telegram `sendMessage` (аналогічно клієнтському outbound, але **інший** endpoint-конфіг).
- [x] **Мінімізація ПІБ у тексті каналу** або узгоджена політика (ініціали, id партнера, посилання на Discuss) — узгодити з RODO та внутрішнім регламентом.
- [x] Доступ до каналу лише у **затверджених** акаунтів; при виході співробітника — прибрати з каналу (allowed user IDs policy check).

**Плутанина з § 13:** канал для клієнтських розсилок / купона **не** замінює **внутрішній операційний** канал; це різні чати й різні токени/політики.

---

## 9. CRM, теги, аналітика

- [x] Опція створення **CRM lead** при новому контакті з месенджера
- [x] Поле тегів на **mail.message** (розмітка діалогів): додано runtime-tagging останнього клієнтського повідомлення для objection / purchase_intent / handoff.
- [x] De-dup внутрішніх purchase-confirmed подій між `sale.order` / `payment.transaction` / `account.move` (вікно configurable: `purchase_dedup_minutes`, dedup по partner + ref/amount).
- [x] Звітність: конверсії, час відповіді, причини відмови/заперечень, джерело каналу (новий екран Operations → CRM Analytics).
- [x] Інтеграція цілей з **маркетинговими кампаніями** Meta (додано goals/fact/achievement у CRM Analytics).

---

## 10. Безпека, відповідність, операційка

- [x] Токени в **Integrations** + обмеження прав; fallback у `ir.config_parameter`
- [x] Перевірка підпису Meta (за наявності App Secret)
- [x] Шифрування секретів (Enterprise / custom) — за політикою безпеки (`docs/SECRET_ENCRYPTION_POLICY.md`).
- [x] Журнал аудиту змін промптів і правил (модель `omni.prompt.audit`, автоматичний лог змін з Settings).
- [x] **RODO / GDPR:** короткий consent-блок у чаті (контролер/мета/посилання) додано в runtime-поведінку бота.
- [x] **Діти:** мінімізація даних додано в policy/system prompt + retention/purge-політика (`retention_child_data_days`, daily purge cron, `docs/CHILD_DATA_RETENTION_POLICY.md`).
- [x] **Документи на сайті** (оферта, RODO, cookies, устава кампу / охорона дітей тощо) — **версійність**; бот отримує **дозволений набір URL + коротких цитат**, затверджених юристом.
- [x] Відповідальність **юридичної особи** — усі автоформулювання погоджені з власником процесу (`legal_approved_owner` + approved snippets).

---

## 11. Експлуатація та якість інженерії

- [x] CI: додано GitHub Actions pipeline (compile + unit tests for webhook parsers + contract regression checks) + lint/tooling expansion (`ruff check`).
- [x] Автотести критичних парсерів webhook (pure parser unit tests: Telegram update_id, Meta mid).
- [x] Staging з тестовою сторінкою Meta (процедура і чекліст: `docs/STAGING_META_TEST_PAGE.md`).
- [x] Runbook: падіння **Ollama**, нестача RAM/GPU, rate limit Graph API, прострочений токен, (опційно) OpenAI (розширено SOP у `OPERATIONS_RUNBOOK.md`).
- [x] Runtime smoke automation: додано `scripts/odoo_runtime_smoke.py` + операційне використання в staging/prod чеклістах.

---

## 12. Утримання в межах 24-годинного вікна (Meta / WhatsApp тощо)

**Ціль:** максимально використовувати **дозволене вікно** після діалогу з реклами; кожна **нова відповідь клієнта** зазвичай **оновлює** лічильник вікна — тому контрольовані, **не дратівні** дотики збільшують сумарний час контакту.

**Продуктова логіка (узгодити з маркетингом / юристом):**

- [x] Після **першої змістовної бесіди** — реалізовано **одне** нагадування приблизно **на ~20-й годині** від останнього **вхідного** повідомлення клієнта (за конфігом), якщо ще немає handoff.
- [x] Текст нагадування: реалізовано **м’яке уточнення**; текст винесено в ICP-параметр для редагування без релізу.
- [x] **Анти-спам:** зафіксовано "one reminder per inbound cycle" + matrix кулдаунів між типами (reminder/FOMO/last-call) і global cooldown у Settings.
- [x] Якщо клієнт **відповів** — цикл скидається (новий inbound скидає marker нагадування), вікно продовжується за тим самим правилом.
- [x] Технічно: додано **cron-планувальник** з прив’язкою до `discuss.channel` + `omni_provider` + timestamp останнього inbound; перевірка max-вікна платформи (конфіг).
- [x] Логування: додано окремий журнал `omni.outbound.log` з `provider/thread/status_code/ok/error` для outbound delivery аудиту.

---

## 13. Стимул переходу в Telegram (знижка 5% та канал для розсилок)

**Не плутати** з **§ 8.1** — там **внутрішній** канал для менеджера та керівника (операційні сповіщення). Тут — **клієнтський** Telegram (бот, купон, маркетингові повідомлення за згодою).

**Бізнес-ідея:** у **Telegram** немає обмеження **24-годинного вікна** як у Meta/WhatsApp для типового сценарію бота — зручніше **довгі нуртування**, нагадування про акції, **розсилки** (за згодою). **5% знижки** за перехід — сильна мотивація, якщо коректно оформити в обліку.

**Узгоджено для таборів (оновлено):** знижка **лише на продукт «табір»**; клієнт самостійно переходить у публічний Telegram-канал `@campscouting` і бере актуальний код з pinned/latest поста.

**Оцінка (для стейкхолдерів):** рішення **логічне** для вашої моделі (реклама в Meta → швидко «дотягнути» до каналу без вікна). Ризики: **маржа**, зловживання (один клієнт — кілька акаунтів), узгодження з бухгалтерією/договором оферти. Компенсація: **обмеження продуктом** + **одноразовість** купона за правилами.

**Чекліст реалізації:**

- [x] Правила акції в **Odoo**: купон/промо «−5%» валідується через `omni.promo` (активність/дати/код/scope) і застосовується лише до camp lines (та whitelist `product_tmpl_ids`, якщо задано).
- [x] Генерація/видача унікального коду **не використовується**: код публічний у Telegram-каналі `@campscouting` (business rule).
- [x] Умова отримання коду: клієнт відкриває публічний канал `@campscouting` і копіює код (без персонального `opt-in` прапорця).
- [x] Повідомлення в Meta/IG: **прозора оферта** runtime-правилом у `omni.ai` (лише табір, як отримати код у `@campscouting`, як застосувати при реєстрації).
- [x] **Згода на розсилки** в Telegram: runtime opt-in/opt-out команди (`/subscribe`, `/unsubscribe`) з фіксацією в `res.partner`.
- [x] Сегментація для розсилок: wizard `Telegram Broadcast` з `only_opted_in` + виключення recent recipients (frequency cap by days).
- [x] Аналітика: у `CRM Analytics` додано метрики переходів в Telegram (`tg_new_contacts`), використання купона (`coupon_redemptions_count`) і ROMI (revenue vs ad spend).

---

## 14. Оцінка senior: що ще критично важливо для ТЗ (часто недоговорюють)

*Нижче — теми, без яких продакшен легко «ламається» або створює юридичний/репутаційний ризик, навіть якщо функціонал «здається готовим».*

### 14.1 Надійність вебхуків і черг

- [x] **Ідемпотентність:** Meta/Telegram можуть **повторно доставити** той самий webhook — потрібен захист від **дублікатів** повідомлень у Discuss (ключ: `update_id` / `message.mid` / hash payload + збереження «вже оброблено»).
- [x] **Таймаут HTTP-воркера Odoo:** виклик LLM може тривати десятки секунд — ризик **504** у клієнта webhook. Краще **швидко відповісти 200** і віддати обробку в **чергу** (`queue_job` / cron / окремий воркер), або чітко підняти `limit_time_*` nginx/worker з розумінням ризиків.
- [x] **Повторні спроби відправки** у Meta (і Telegram outbound) при тимчасових помилках HTTP / 429 / 5xx з backoff у `omni.bridge`.

### 14.2 Гонки «бот vs менеджер»

- [x] Явна **блокуюча семантика:** manager-session lock + FSM-safe transition writes; бот не шле конкурентну відповідь під час активної сесії менеджера.
- [x] Порядок повідомлень у клієнта: runtime guard проти bot-vs-manager колізій + anti-duplicate outbound в короткому вікні.

### 14.3 Персональні дані, діти, контекст LLM

- [x] **Мінімізація:** policy/rules у prompt + runtime retention/purge + базові DPIA категорії в `docs/DPIA_DATA_CATEGORIES.md`.
- [x] **Логи:** додано маскування email/phone у bridge-логах + retention cron для `mail.message`/`omni.webhook.event`.
- [x] **Право на видалення:** `res.partner.action_omni_right_to_erasure` + окремий SOP з platform limitations (`docs/RIGHT_TO_ERASURE_SOP.md`).

### 14.4 Операційна готовність

- [x] **Kill switch:** одна настройка «вимкнути автовідповіді LLM / усіх ботів» без деплою коду.
- [x] **Staging** з тим самим набором **кастомних модулів**, що прод: додано bootstrap/runbook (`docs/STAGING_RUNTIME_BOOTSTRAP.md`) + runtime smoke критерії.
- [x] **Версії:** зафіксувати Odoo, hash кастомних репо, **модель Ollama** — відтворювані релізи (release fingerprint поля + policy в runbook).
- [x] **Бекап і відновлення:** прогін відновлення БД + filestore; чи зберігаються **чати** як критичний актив (`docs/BACKUP_RESTORE_DRILL.md`).

### 14.5 Безпека та зловживання

- [x] Обмеження **розміру payload** вебхука (`webhook_max_body_bytes`, 413) + best-effort app-layer rate limit по IP (`webhook_rate_limit_per_minute`, 429) з рекомендацією infra-layer.
- [x] **Ротація** токенів Meta/Telegram; хто відповідає за календар оновлення (owner + next date у Settings, SOP в runbook).

### 14.6 Якість і відповідальність перед клієнтом

- [x] **Fallback**, коли Ollama/LLM недоступний: короткий шаблон «зараз з’єднаємо з менеджером» / години, а не мовчання.
- [x] **Регресія після змін** у кастомних модулях: contract tests + runtime smoke automation (`scripts/odoo_runtime_smoke.py`) зафіксовані як обов’язковий post-deploy gate.

---

## 15. Висновок для стейкхолдерів

**Стан на 2026-04-09:** чекліст вище закритий повністю (**160 × `[x]`**, без `[~]` / `[ ]`). У коді `omnichannel_bridge` реалізовано омніканальний контур: **Meta, Telegram, WhatsApp (Cloud + Twilio), Viber**, міст у **Discuss (`discuss.channel`)**, **черга ШІ**, **livechat** з entry-flow (тема + pre-chat імʼя/контакт + lead), **внутрішній Telegram для менеджерів (§ 8.1)** через `omni.notify`, **суворе підґрунтя з ORM**, домен таборів (місця з truth-sync, резерв, купон, промо/страхування/legal docs у контексті), **RODO/мінімізація/retention/право на видалення**, **CI + contract tests + runtime smoke** як post-deploy gate.

Чекліст залишається **живим документом**: при зміні бізнес-правил або нових каналах — додавати пункти й знову вести `[x]` / `[~]` / `[ ]`.

**Важливо:** LLM залишається **парафразером** поверх фактів; **джерело правди — Odoo + затверджені політики та записи в системі**. Додаткова гарантія якості — регулярні **prod/staging прогони**, юридичний sign-off формулювань і людина в циклі на чутливих темах.

---

## 16. Оцінка виконання ТЗ (стан на 2026-04-09)

### Загальна оцінка

- **За чеклістом у репозиторії: 100%** (усі пункти `[x]`).
- **Операційна готовність** залежить від регулярних перевірок на staging/prod (webhooks, Ollama, livechat UX, платежі) — це не скасовує закриття ТЗ як специфікації, але визначає стабільність у бою.

### Розбивка по блоках (актуалізовано)

1. **Core платформа та інтеграція каналів — ~95%**
   - зроблено: Meta/Telegram/WhatsApp/Viber, idempotency, AI queue, fallback, livechat bridge + pre-chat, 24h reminders, outbound log.
   - підтримка: моніторинг провайдерів і infra rate-limit поверх app-layer.

2. **Sales/AI логіка і кваліфікація — ~95%**
   - зроблено: memory/profile, discovery, strict grounding, FSM guard + журнал `omni.stage.event`, editor переходів `omni.stage.transition`, manager lock, objection/moderation policy UI.

3. **Бізнес-специфіка таборів — ~95%**
   - зроблено: prod mapping audit, truth-sync місць, reserve flow, купон E2E + redemption + campaign metadata, промо/страхування/legal у grounding.

4. **Security/RODO/Operations/CI — ~95%**
   - зроблено: consent, retention/purge, PII masking, erasure SOP, secret policy docs, backup drill doc, CI, contract tests, `scripts/odoo_runtime_smoke.py`, staging bootstrap doc, prod livechat smoke report.

### Важливе уточнення

- Повнота **юридичного підпису** текстів consent/оферти — зона відповідальності власника процесу, не лише коду.
- Рекомендовано планово повторювати **PROD_COPY / smoke** після кожного значного релізу модуля або оновлення кастомних аддонів, що впливають на місця/події/оплату.

---

## 17. Mobile-first message policy (mandatory)

Цей блок є **обовʼязковою частиною ТЗ** для всіх клієнтських каналів (Meta/IG, Telegram, WhatsApp, Viber, livechat).

### 17.1 Канальні ліміти та операційні caps

- Орієнтовні platform hard limits (потрібно періодично звіряти з офіційними docs провайдера):
  - Telegram Bot API: до ~4096 символів на повідомлення.
  - WhatsApp Business/Cloud API (text): до ~4096 символів.
  - Meta Messenger/Instagram: різні обмеження за типом повідомлення/API; використовувати консервативні короткі тексти.
- **Операційні caps у нас (строгіші за platform limits):**
  - Meta/Instagram/WhatsApp/Viber: до ~320 символів в одному повідомленні.
  - Site livechat: до ~360 символів.
  - Telegram: до ~500 символів.

### 17.2 UX правила для діалогу

- Один меседж = одна думка.
- Один бот-відповідь = максимум одне питання до клієнта.
- Заборонені “стіни тексту” та багатопунктні маркетингові блоки одним куском.
- Якщо потрібно більше пояснень — розбивати на 2-3 короткі повідомлення.
- У mobile-каналах пріоритет читабельності: короткі абзаци, прості CTA, мінімум прокрутки.

### 17.3 Data-aware flow (не питати те, що вже відомо)

- Перед кожним запитанням перевіряти наявні поля профілю (`user_fullname`, `user_email`, `user_phone` та інші з контексту).
- Якщо поле вже заповнене — пропускати крок і переходити далі.
- Якщо поле порожнє — ставити тільки одне цільове запитання.

### 17.4 Compliance та безпечне кодування

- Тексти мають коректно відображатися в різних месенджерах (UTF-8, без “битих” символів).
- Емодзі дозволені як маркери структури повідомлення (за потреби), але без перевантаження.
- RODO/Privacy посилання давати коротким рядком, без довгих юридичних вставок у кожній відповіді.

---

## 18. Silence recovery policy (24/7 sales psychology)

Ціль: не втрачати лідів, які написали і “зникли”, та коректно повертати їх у діалог без спаму.

### 18.1 Сигнали мовчання (поведінкові)

- `client_silent_after_bot`: клієнт не відповів після відповіді бота.
- `client_returned_late`: клієнт повернувся після довгої паузи (пізно ввечері/вночі або через кілька днів).
- `handoff_silent`: клієнт просив менеджера, але не залишив контакт / не завершив крок.

### 18.2 Обов’язкові SLA-тригери

- **T+15 хв** після останньої відповіді бота і без клієнтської відповіді:
  - короткий “nudge” (1 речення, 1 CTA), без маркетингових блоків.
- **T+12 год**:
  - другий nudge з варіантами швидкого вибору (ціна/дати/місця/контакт).
- **T+48 год**:
  - фінальний м’який follow-up + опція “пауза/відписка”.

### 18.3 Нічний режим (24/7)

- Бот відповідає вночі так само швидко, але:
  - без агресивних продажних формулювань;
  - з фразою “менеджер підключиться у робочі години”, якщо потрібен handoff.
- Якщо вночі клієнт дає контакт — фіксуємо lead одразу, нотифікуємо менеджера в ранковий слот.

### 18.4 Анти-спам та частотні обмеження

- Не більше 2 follow-up повідомлень за 48 год на один активний діалог.
- Якщо клієнт відповів — лічильник silence-follow-up скидається.
- Якщо клієнт явно відмовився/“неактуально” — follow-up не надсилати.

### 18.5 Контент правилa для silence follow-up

- Максимум одне питання в повідомленні.
- Довжина за mobile caps із §17.
- Тон: емпатичний, без тиску (“зручно/підкажу/можу допомогти”).
- У разі “дорого/подумаю/сам напишу”:
  - не тиснути, дати короткий наступний крок та контрольну точку повернення.

### 18.6 KPI для відділу продажів

- `response_recovery_15m`: частка діалогів, де клієнт відповів після першого nudge.
- `silence_to_contact`: частка мовчазних діалогів, що завершилися контактом.
- `silence_to_handoff`: частка мовчазних діалогів, що перейшли в менеджерський handoff.
- `overfollowup_rate`: частка порушень частотного ліміту (має бути ~0).

---

## 19. Weekly auto-audit for sales behavior (mandatory)

- Щопонеділка вночі модуль має автоматично аналізувати розмови за попередні 7 днів.
- Результат зберігається як аудит-звіт в Odoo (`Omnichannel -> Operations -> Conversation Audits`).
- Мінімальні категорії для виявлення:
  - дорого (`price_expensive`);
  - подумаю/пізніше (`think_later`);
  - сама/сам напишу (`self_followup`);
  - треба порадитися з родиною (`need_family_consult`);
  - скажу/запитаю у дитини (`ask_children_first`);
  - далеко/логістика (`distance_far`);
  - мовчання після відповіді бота або менеджера (`client_silence_threads`);
  - пізня відповідь клієнта (`delayed_client_reply_threads`).
- Мінімальні manager-error індикатори:
  - повільна відповідь менеджера після явного запиту менеджера (>2h);
  - відсутня відповідь менеджера після явного запиту менеджера (>24h).
- Кожен звіт має містити агреговані лічильники та приклади повідомлень/тредів для ручного розбору.
